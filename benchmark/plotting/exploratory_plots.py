"""Plots that help us get a sense of the behavior of internals of the algorithm."""

def plot_joint_W_distribution():
    """Plot a 2D histogram (with 1D marginals?) of the W_F and W_R distributions.
    If each marginal is Gaussian, and they are jointly Gaussian,
    then we probably don't have much to gain from, say, using the medians rather than
    the means.
    """
    pass